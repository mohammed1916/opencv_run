# configure (from project root) and then build

```
cmake -S . -B build
cmake --build build --config Debug
```


### Run debug

```
./build/debug/OpenCVTest.exe
```

where OpenCVTest.exe must exist in the above mentioned dir
